{
    "buildCommand": "npm install @rollup/rollup-linux-x64-gnu --save-optional && npm run db:push && npm run build",
    "outputDirectory": "dist/public",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.ts"
        }
    ]
}
